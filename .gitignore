# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Secrets & local tooling
.env
.env.*
!.env.example
# Public-by-design client config (Supabase URL + anon key) — the publish
# pipeline builds without a dev .env and must still inline these.
!.env.production
.claude/
.vercel/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local design reference images (not app assets)
Images/
